@model List<User>

@{
    ViewData["Title"] = "Users List";
}

<h2>Users List</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Genre</th>
            <th>Published</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model.Users)
        {
            <tr>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.Genre</td>
                <td>@book.PublishedDate.ToString("yyyy-MM-dd")</td>
            </tr>
        }
    </tbody>
</table>
